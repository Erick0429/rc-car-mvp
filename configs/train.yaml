dataset:
  root: datasets          # Webots 데이터 수집 경로 (episode_XXXX/ 폴더들)
  image_size: [160, 90]   # 모델 입력 크기 [W, H]
  batch_size: 32
  train_split: 0.8

model:
  name: small_cnn
  outputs: 2

train:
  epochs: 20
  lr: 0.001
  weight_decay: 0.0001
  steering_weight: 1.0
  throttle_weight: 0.5
