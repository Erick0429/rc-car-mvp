dataset:
  root: data/raw
  image_size: [160, 90]
  batch_size: 32
  train_split: 0.8

model:
  name: small_cnn
  outputs: 2

train:
  epochs: 20
  lr: 0.001
  weight_decay: 0.0001
  steering_weight: 1.0
  throttle_weight: 0.5
